const { expect } = require('chai');

describe("Token contract", function () {
    it("Hello test works", async function () {
      const name = 'gameing contract';

      expect(name).to.be.string
    });
  });